DirectoryIndex index.php

<IfModule mod_rewrite.c>
    RewriteEngine On
    # Rediriger la racine vers index.php pour que l'URL affichée soit toujours .../index.php
    RewriteCond %{REQUEST_URI} ^/?$
    RewriteRule ^$ index.php [R=301,L]
</IfModule>
